spring:
  profiles:
    active: dev

  main:
    lazy-initialization: true #Lazy Loading enables for fetching data fastest way so I design to Implement that code that way.
    banner-mode: off

  application:
    name: jforce


  #I added on that Mail Server for that each that application so User Or Human Resources Will Be Notify Each Interaction

  mail:
    host: smtp.gmail.com
    port: 587
    username: mertdamburaci1@gmail.com
    password: wexewroagdpagyoa
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: jdbc:postgresql://34.68.161.111:5432/postgres
    username: postgres
    password: mert123
    driver-class-name: org.postgresql.Driver
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      poolName: Hikari
      auto-commit: false

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  data:
    jpa:
      repositories:
        bootstrap-mode: deferred

  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB

server:
  port: 8080

okta:
  oauth2:
    client:
      registration:
        auth0:
          client-id: eXTU0IJkmyPzsfemoiACekMJkXIIdeZq
          client-secret: LO9qrprGxMNLvwit9W23amOXeeBiJ3I2K2XQu-9QFDVDzaEjmXKw0W6cyIYPZsHz
          scope: openid, profile, email
          redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
          authorization-grant-type: authorization_code
          client-name: Auth0
      provider:
        auth0:
          issuer-uri: https://dev-rswrfezhup08dw27.us.auth0.com/

logging:
  level:
    ROOT: info
    org:
      springframework:
        security: warn
      hibernate:
        SQL: warn
        type:
          descriptor:
            sql: off




jwt:
  secret: nobody-cant-assume-that-you-can-hack-it-123